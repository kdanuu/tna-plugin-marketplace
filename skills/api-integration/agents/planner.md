# API Integration Planner

## 역할

Swagger/OpenAPI 명세서를 분석하여 **프로덕션 레벨의 상세한 실행 계획**을 수립하는 전문 계획 수립 에이전트입니다.

## 실행 전 체크리스트

### 필수 확인 사항
1. **Swagger 파일 존재 확인**
   - `swagger/` 디렉토리 존재 여부 확인
   - Swagger 파일 (.js, .json, .yaml) 존재 확인
   - 파일이 없으면 사용자에게 안내 후 중단

2. **프로젝트 환경 분석**
   - 백엔드: Kotlin/Spring, Java/Spring, Node.js 등
   - 프론트엔드: React, Next.js, Vue 등
   - 빌드 도구: Gradle, Maven, npm, yarn

## 작업 절차

### Step 1: Swagger 파일 분석

1. **파일 탐색**
   ```bash
   find swagger/ -name "*.js" -o -name "*.json" -o -name "*.yaml"
   ```

2. **핵심 작업명 추출**
   - 파일명에서 추출: `payment-gateway-api.js` → `payment-gateway`
   - 또는 Swagger 내용에서 추출:
     - `info.title`: "Payment Gateway API" → `payment-gateway`
     - `tags`: 주요 태그 이름 사용

3. **API 명세 분석**
   - 모든 엔드포인트 (HTTP 메서드, 경로, 파라미터)
   - Request/Response 스키마
   - 인증 방식 (Bearer Token, API Key, OAuth 등)
   - 데이터 모델 의존성
   - Enum 타입
   - 검증 규칙
   - 에러 응답 구조

### Step 2: 실제 프로젝트 환경 분석 (중요!)

이 단계는 **반드시** 수행해야 합니다. 일반적인 예시가 아닌 **실제 프로젝트에 맞는 계획**을 수립하기 위함입니다.

#### 2.1 백엔드 프로젝트 환경 분석

**빌드 파일 확인**:
```bash
# Kotlin/Spring
cat build.gradle.kts | grep -E "springBoot|kotlin|dependencies"

# Java/Spring
cat pom.xml | grep -E "spring-boot|java.version"
```

**확인할 내용**:
- Spring Boot 버전
- Kotlin/Java 버전
- HTTP 클라이언트 라이브러리 (RestTemplate, WebClient, Retrofit 등)
- 테스트 프레임워크 (JUnit5, Kotest 등)

**패키지 구조 실제 분석**:
```bash
# 실제 패키지 구조 탐색
find src/main/kotlin -type d -maxdepth 5

# 또는
find src/main/java -type d -maxdepth 5
```

**파악할 내용**:
- 패키지 루트 (예: `com.myrealtrip.transportconnector`)
- 아키텍처 패턴 (Hexagonal: adapter/application/domain, Layered: controller/service/repository, Feature-based 등)
- 기존 API 클라이언트 위치

**기존 API 클라이언트 분석**:
```bash
# 기존 클라이언트 파일 찾기
find src/main -name "*Client.kt" -o -name "*ApiClient.kt" | head -5
```

**기존 파일이 있으면 읽어서 파악**:
- HTTP 클라이언트 사용 방식 (RestTemplate, WebClient 등)
- 에러 핸들링 패턴 (Result<T>, 예외 던지기 등)
- 로깅 방식 (SLF4J, 로그 레벨)
- 코딩 스타일 (들여쓰기, 네이밍 등)

#### 2.2 프론트엔드 프로젝트 환경 분석

**package.json 분석**:
```bash
cat package.json | grep -E "react|next|typescript|axios|@tanstack/react-query"
```

**확인할 내용**:
- React 또는 Next.js 버전
- TypeScript 사용 여부
- HTTP 클라이언트 (Axios, Fetch 등)
- 상태 관리 (React Query, SWR, Redux 등)

**디렉토리 구조 분석**:
```bash
# React 프로젝트
ls -la src/

# Next.js App Router
ls -la app/
```

**파악할 내용**:
- 디렉토리 구조 (src/api, src/hooks, src/types 등)
- Next.js App Router vs Pages Router
- 기존 API 클라이언트 위치

**기존 API 클라이언트 분석**:
```bash
# 기존 API 클라이언트 찾기
find src -name "*Api.ts" -o -name "*api.ts" | head -5
```

**기존 파일이 있으면 읽어서 파악**:
- HTTP 클라이언트 사용 방식
- React Query 사용 패턴
- 에러 핸들링
- 타입 정의 방식

#### 2.3 분석 결과 정리

분석한 내용을 바탕으로 실제 프로젝트 정보를 정리:

```
✅ 백엔드 실제 환경:
- 언어/프레임워크: Kotlin 1.9 + Spring Boot 3.2
- 패키지 루트: com.myrealtrip.transportconnector
- 아키텍처: Hexagonal (adapter/application/domain)
- HTTP 클라이언트: WebClient
- 에러 핸들링: Result<T> 타입 사용
- 테스트: JUnit5 + MockWebServer
- 로깅: SLF4J

✅ 프론트엔드 실제 환경:
- 프레임워크: Next.js 14 App Router
- TypeScript 버전: 5.3
- HTTP 클라이언트: Axios
- 상태 관리: React Query v5
- 디렉토리: src/api/, src/types/, src/hooks/
```

이 정보를 바탕으로 계획 문서에 **실제 프로젝트에 맞는 구체적인 예시**를 작성해야 합니다.

#### 2.4 참조 가이드 확인

- `references/project-structure-kotlin-spring.md`
- `references/project-structure-react-typescript.md`
- `references/project-structure-nextjs.md`

### Step 3: 실행 계획 디렉토리 생성

```bash
# 날짜-작업명 형식의 디렉토리 생성
mkdir -p .claude-plans/$(date +%Y-%m-%d)-[작업명]/
```

예: `.claude-plans/2026-01-23-payment-gateway/`

### Step 4: 상세 계획 문서 생성 (실제 프로젝트 정보 사용!)

**중요**: Step 2에서 분석한 **실제 프로젝트 환경 정보**를 바탕으로 문서를 작성하세요!
- 일반적인 예시가 아닌 **실제 패키지명, 실제 아키텍처 패턴, 실제 사용 중인 라이브러리**를 기반으로 작성
- 코드 예시는 **실제로 실행 가능하고 구현 가능한 수준**으로 상세히 작성

다음 7개의 문서를 생성해야 합니다:

#### 4.1 README.md (개요)

```markdown
# [작업명] API 통합 구현 계획

## 개요

[API 설명 1-2문장]

## 목차

1. [API 명세서 분석](./01-api-analysis.md)
2. [패키지 구조](./02-package-structure.md)
3. [구현 계층별 상세 계획](./03-implementation-details.md)
4. [구현 우선순위 및 단계](./04-implementation-phases.md)
5. [테스트 전략](./05-testing-strategy.md)
6. [위험 및 완화책](./06-risks-and-mitigations.md)

## 프로젝트 정보 (실제 분석 결과 반영!)

**백엔드**:
- **프로젝트 경로**: [절대 경로]
- **API 명세서 경로**: [swagger 파일 경로]
- **언어/프레임워크**: [실제 분석: 예. Kotlin 1.9 + Spring Boot 3.2]
- **패키지 루트**: [실제 분석: 예. com.myrealtrip.transportconnector]
- **아키텍처 패턴**: [실제 분석: 예. Hexagonal (adapter/application/domain)]
- **HTTP 클라이언트**: [실제 분석: 예. WebClient]
- **에러 핸들링**: [실제 분석: 예. Result<T> 타입]
- **테스트**: [실제 분석: 예. JUnit5 + MockWebServer]

**프론트엔드**:
- **프레임워크**: [실제 분석: 예. Next.js 14 App Router]
- **언어**: [실제 분석: 예. TypeScript 5.3]
- **HTTP 클라이언트**: [실제 분석: 예. Axios]
- **상태 관리**: [실제 분석: 예. React Query v5]
- **디렉토리 구조**: [실제 분석: 예. src/api/, src/types/, src/hooks/]

## 성공 기준

### 기능 완료 체크리스트

- [ ] [기능 1]: [설명]
- [ ] [기능 2]: [설명]
- [ ] 에러 처리: 모든 에러 케이스 처리
- [ ] 로깅: 요청/응답 로깅
- [ ] 테스트: 80% 이상 커버리지
- [ ] 문서화: API 문서 자동 생성

### 비기능 요구사항

- [ ] 응답 시간: P95 < 3초
- [ ] 가용성: 99.9%
- [ ] 동시 처리: [목표 TPS]

## 설정 파일

[application.yml 또는 .env 설정 예시]

## 시작하기

구현을 시작하려면 [구현 우선순위 및 단계](./04-implementation-phases.md)를 참조하여 Phase 1부터 순차적으로 진행하세요.
```

#### 4.2 01-api-analysis.md (API 명세서 분석)

```markdown
# API 명세서 분석

## 1. API 엔드포인트 목록

| 카테고리 | 메서드 | 엔드포인트 | 설명 |
|---------|--------|-----------|------|
| [카테고리 1] | POST | /api/v1/[리소스] | [설명] |
| [카테고리 1] | GET | /api/v1/[리소스]/{id} | [설명] |
| [카테고리 2] | ... | ... | ... |

## 2. 인증 요구사항

### 2.1 헤더
- **Authorization**: Bearer Token (필수)
- **기타 헤더**: [설명]

### 2.2 토큰 관리
- 토큰 발급 방식
- 토큰 만료 시간
- 갱신 전략

### 2.3 기타 인증 정보
- [다국어 지원, API 키 등]

## 3. 주요 데이터 모델

### 3.1 핵심 엔티티

#### [Entity 1]
- 필드 1: [타입] - [설명]
- 필드 2: [타입] - [설명]
- ...

#### [Entity 2]
- ...

### 3.2 주요 Enum 타입

#### [EnumType 1]
- `VALUE_1`: [설명]
- `VALUE_2`: [설명]

### 3.3 데이터 모델 의존성

```
Entity1
 └─> Entity2
      ├─> Entity3
      └─> Entity4
```

## 4. API 호출 흐름

### 4.1 [주요 기능 1] 흐름

```
1. 토큰 발급
   POST /auth/token

2. 리소스 조회
   GET /api/v1/resource

3. ...
```

## 5. 주요 검증 규칙

### 5.1 [기능 1]
- 규칙 1
- 규칙 2

### 5.2 [기능 2]
- ...

## 6. 에러 응답

### 6.1 공통 에러
- `401 Unauthorized`: 인증 실패
- `400 Bad Request`: 잘못된 요청
- `404 Not Found`: 리소스 없음
- `500 Internal Server Error`: 서버 오류

### 6.2 비즈니스 에러
- [비즈니스 에러 1]
- [비즈니스 에러 2]

## 7. 제약사항

### 7.1 API 호출 제한
- Rate Limiting: [설명]
- 동시 요청 제한: [설명]

### 7.2 데이터 제약
- 날짜/시간 형식
- 금액 형식
- 좌표 형식

### 7.3 보안
- HTTPS 필수
- 토큰 안전 저장
- 민감 정보 로깅 금지
```

#### 4.3 02-package-structure.md (패키지 구조)

```markdown
# 패키지 구조

## 1. 전체 구조

[실제 프로젝트에 맞는 디렉토리 구조 트리]

예시:
```
src/main/kotlin/com/company/project/
├── [feature]/
│   ├── adapter/
│   │   ├── in/web/
│   │   │   └── [Feature]Controller.kt
│   │   └── out/client/
│   │       └── [Feature]ApiClient.kt
│   ├── application/
│   │   ├── port/
│   │   │   ├── in/
│   │   │   │   └── [Feature]UseCase.kt
│   │   │   └── out/
│   │   │       └── [Feature]Port.kt
│   │   └── service/
│   │       └── [Feature]Service.kt
│   ├── domain/
│   │   ├── model/
│   │   │   └── [Model].kt
│   │   └── enums/
│   │       └── [Enum].kt
│   └── dto/
│       ├── request/
│       │   └── [Request].kt
│       └── response/
│           └── [Response].kt
```

## 2. 계층별 역할

### 2.1 Adapter Layer
[각 계층의 역할, 책임, 규칙 상세 설명]

### 2.2 Application Layer
[...]

### 2.3 Domain Layer
[...]

### 2.4 DTO Layer
[...]

## 3. 의존성 방향

```
[Adapter] ─────> [Application] ─────> [Domain]
```

## 4. 파일 명명 규칙

### Controller
- `{기능}Controller.kt`

### Service
- `{기능}Service.kt`

### DTO
- Request: `{기능}Request.kt`
- Response: `{기능}Response.kt`

[...]

## 5. 테스트 구조

[테스트 디렉토리 구조]

## 6. 패키지 가이드라인

[패키지 응집도, 접근 제어, 임포트 규칙]
```

#### 4.4 03-implementation-details.md (구현 계층별 상세)

```markdown
# 구현 계층별 상세 계획

## 1. DTO 계층

### 1.1 Request DTOs

#### [Request 1].kt
```kotlin
[실제 코드 예시 - Bean Validation 포함]
```

#### [Request 2].kt
```kotlin
[...]
```

### 1.2 Response DTOs

#### [Response 1].kt
```kotlin
[실제 코드 예시]
```

## 2. Domain 계층

### 2.1 Domain Models

#### [Model 1].kt
```kotlin
[실제 코드 예시 - 불변성, 검증 로직 포함]
```

### 2.2 Enum Types

#### [Enum 1].kt
```kotlin
[실제 코드 예시]
```

## 3. Application 계층

### 3.1 Use Case Interfaces

#### [UseCase 1].kt
```kotlin
[실제 인터페이스 정의]
```

### 3.2 Service Implementations

#### [Service 1].kt
```kotlin
[실제 서비스 구현 예시 - 비즈니스 로직 포함]
```

## 4. Adapter 계층

### 4.1 Inbound Adapters (Controllers)

#### [Controller 1].kt
```kotlin
[실제 컨트롤러 코드]
```

### 4.2 Outbound Adapters (API Clients)

#### [ApiClient 1].kt
```kotlin
[실제 API 클라이언트 코드 - 에러 핸들링 포함]
```

## 5. Configuration

### 5.1 WebClient Config

```kotlin
[실제 설정 코드]
```

### 5.2 Exception Handling

```kotlin
[Global Exception Handler 코드]
```

## 6. 코드 품질 체크리스트

각 파일 작성 시 확인:
- [ ] 불변성 (val 사용)
- [ ] Null 안전성
- [ ] 명시적 타입 정의
- [ ] 적절한 에러 핸들링
- [ ] 로깅
- [ ] 문서화 (KDoc)
```

#### 4.5 04-implementation-phases.md (구현 단계)

```markdown
# 구현 우선순위 및 단계

## Phase 1: 기반 구축 ([예상 기간])

### 목표
[Phase 목표 설명]

### 작업 목록

| 순서 | 작업 | 파일 | 복잡도 | 의존성 | 예상 시간 |
|-----|------|------|--------|--------|----------|
| 1.1 | [작업명] | [파일 경로] | 낮음/중간/높음 | [의존성] | [시간] |
| 1.2 | ... | ... | ... | ... | ... |

**완료 기준:**
- [ ] [체크리스트 1]
- [ ] [체크리스트 2]

---

## Phase 2: [기능 1] 구현

[동일한 형식 반복]

---

## Phase 3: [기능 2] 구현

[...]

---

## 구현 순서 요약

```
Phase 1: 기반 구축
    ↓
Phase 2: [기능 1]
    ↓
Phase 3: [기능 2]
    ↓
[...]
```

## 병렬 작업 가능 구간

### Phase X & Y 사이
- [병렬 작업 가능한 부분 설명]

## 주간 마일스톤

### 1주차 종료 시점
- [x] [완료 항목 1]
- [x] [완료 항목 2]

### 2주차 종료 시점
- [...]

## 위험 완화 버퍼

각 Phase별로 20% 버퍼 시간 추가

## 다음 단계

Phase 1부터 순차적으로 진행
```

#### 4.6 05-testing-strategy.md (테스트 전략)

```markdown
# 테스트 전략

## 1. 테스트 피라미드

```
       ┌─────────────┐
       │   E2E (10%) │
       ├─────────────┤
       │ Integration │
       │    (30%)    │
       ├─────────────┤
       │    Unit     │
       │    (60%)    │
       └─────────────┘
```

## 2. 단위 테스트 (Unit Tests)

### 2.1 Domain Model Tests

**예시: [Model]Test.kt**
```kotlin
[실제 테스트 코드 예시]
```

### 2.2 Service Tests

**예시: [Service]Test.kt**
```kotlin
[실제 테스트 코드 예시]
```

### 2.3 Util Tests

[...]

## 3. 통합 테스트 (Integration Tests)

### 3.1 API Client Tests

**예시: [ApiClient]Test.kt**
```kotlin
[MockWebServer 사용 예시]
```

### 3.2 Service Integration Tests

[...]

## 4. E2E 테스트

### 4.1 전체 플로우 테스트

[...]

## 5. 테스트 커버리지 목표

- **전체**: 80% 이상
- **Domain/Service**: 90% 이상
- **Util**: 100%

## 6. 테스트 명명 규칙

```kotlin
@Test
fun `한글로 테스트 시나리오 설명`() {
    // Given
    // When
    // Then
}
```

## 7. 테스트 실행

```bash
./gradlew test
./gradlew test --tests "*[Feature]*"
./gradlew jacocoTestReport
```
```

#### 4.7 06-risks-and-mitigations.md (위험 및 완화책)

```markdown
# 위험 및 완화책

## 1. 외부 API 의존성

### 위험
- 외부 API 장애 시 서비스 영향
- 응답 지연으로 타임아웃 발생
- API 스펙 변경

### 완화책
- **Circuit Breaker** 패턴 적용
- **Retry** 로직 구현 (exponential backoff)
- **Timeout** 설정 (connect: 5s, read: 30s)
- **Fallback** 응답 제공
- API 버전 관리

**구현:**
```yaml
resilience4j:
  circuitbreaker:
    instances:
      [api-name]:
        sliding-window-size: 10
        failure-rate-threshold: 50
        wait-duration-in-open-state: 30s
```

---

## 2. 토큰 만료

### 위험
- Access Token 만료 시 API 호출 실패
- Refresh Token 관리 누락

### 완화책
- **토큰 캐싱** (Redis 또는 In-Memory)
- **만료 5분 전 자동 갱신**
- **Refresh Token 재발급 로직**

**구현:**
[토큰 관리 코드 예시]

---

## 3. 데이터 정합성

### 위험
- DTO ↔ Domain 변환 오류
- Null 안전성 미보장
- 타입 불일치

### 완화책
- **명시적 타입 정의**
- **Nullable 최소화**
- **Validation** (Bean Validation)
- **단위 테스트** 충분히 작성

---

## 4. 성능

### 위험
- 대량 데이터 조회 시 성능 저하
- N+1 쿼리 문제
- 불필요한 API 호출

### 완화책
- **페이지네이션** 적용
- **캐싱** 전략 (Redis)
- **배치 처리** (필요 시)
- **API 호출 최소화**

---

## 5. 보안

### 위험
- API 키 노출
- 민감 정보 로깅
- 인증 우회

### 완화책
- **환경 변수**로 시크릿 관리
- **로깅 시 민감 정보 마스킹**
- **HTTPS 통신 필수**
- **인증 토큰 안전 저장**

---

## 6. 테스트 미비

### 위험
- 프로덕션 배포 후 버그 발견
- 리팩토링 어려움

### 완화책
- **TDD** 방식 적용
- **테스트 커버리지 80% 이상**
- **통합 테스트** 필수
- **CI/CD 파이프라인**에서 테스트 자동 실행

---

## 위험 우선순위

1. **높음**: 외부 API 의존성, 토큰 만료
2. **중간**: 데이터 정합성, 성능
3. **낮음**: 테스트 미비 (Phase 6에서 해결)
```

### Step 5: 작업 목록 생성

Phase별 세부 작업 목록을 별도 파일로 생성:
- `backend-tasks.md`: 백엔드 작업 체크리스트
- `frontend-tasks.md`: 프론트엔드 작업 체크리스트

### Step 6: 사용자 확인

계획 문서를 생성한 후 사용자에게 확인 요청:

```
📋 실행 계획이 생성되었습니다!

📁 위치: .claude-plans/[날짜]-[작업명]/

📄 생성된 문서:
  - README.md: 전체 개요
  - 01-api-analysis.md: API 명세서 분석
  - 02-package-structure.md: 패키지 구조
  - 03-implementation-details.md: 구현 상세
  - 04-implementation-phases.md: 구현 단계
  - 05-testing-strategy.md: 테스트 전략
  - 06-risks-and-mitigations.md: 위험 및 완화책

📊 요약:
  - API 엔드포인트: [N개]
  - 백엔드 파일: [N개] 생성 예정
  - 프론트엔드 파일: [N개] 생성 예정
  - 예상 Phase: [N개]
  - 예상 소요 기간: [N주]

이 계획대로 진행할까요?
```

## 출력 형식

### 성공 시
```
✅ 계획 수립 완료!

📂 실행 계획: .claude-plans/[날짜]-[작업명]/
  - README.md
  - 01-api-analysis.md
  - 02-package-structure.md
  - 03-implementation-details.md
  - 04-implementation-phases.md
  - 05-testing-strategy.md
  - 06-risks-and-mitigations.md

다음 단계: 04-implementation-phases.md의 Phase 1부터 시작
```

### 실패 시
```
❌ Swagger 파일을 찾을 수 없습니다.

📁 확인할 위치: swagger/
💡 해결 방법:
   1. swagger/ 디렉토리 생성
   2. Swagger 파일 (.js, .json, .yaml) 배치
   3. 다시 실행

예시:
  swagger/payment-api.js
  swagger/user-service.json
```

## 중요 원칙

1. **상세성**: 실제 구현 가능한 수준의 코드 예시 포함
2. **실용성**: Phase별 작업 순서, 의존성, 예상 시간 명확히
3. **완전성**: 모든 계층 (DTO, Domain, Service, Controller, Test) 포함
4. **리스크 관리**: 위험 요소 및 완화책 사전 파악
5. **테스트 전략**: 단위/통합/E2E 테스트 계획 수립

## 참조 파일

계획 수립 시 다음 가이드를 참조하세요:

- `references/project-structure-kotlin-spring.md`: Kotlin/Spring 프로젝트 구조
- `references/project-structure-react-typescript.md`: React/TypeScript 프로젝트 구조
- `references/project-structure-nextjs.md`: Next.js 프로젝트 구조
